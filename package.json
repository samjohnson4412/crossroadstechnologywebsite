{
  "name": "crossroadstechnology-website",
  "version": "1.0.0",
  "private": true,
  "description": "Static website for Crossroads Technology — Tampa Bay managed IT and low-voltage contractor.",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node build.js",
    "validate": "node build.js && node validate.js",
    "serve": "npx --yes http-server dist -p 8080 -c-1",
    "dev": "node build.js && npx --yes http-server dist -p 8080 -c-1"
  }
}
